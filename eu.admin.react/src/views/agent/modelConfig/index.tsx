import { TableList } from "@/components";
// import FormPage from "./FormPage";

/** 模型配置系统模块；列表由元数据驱动，密钥维护通过专用安全接口。 */
const ModelConfig = () => <TableList moduleCode="AG_MODEL_CONFIG_MNG"
//  DynamicFormPage={FormPage}
  />;

export default ModelConfig;
