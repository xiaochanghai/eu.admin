import React from "react";
import TableList from "../../common/components/TableList";

const Index: React.FC<any> = () => {
  return <TableList moduleCode="SM_USER_MNG" />;
};

export default React.memo(Index);
