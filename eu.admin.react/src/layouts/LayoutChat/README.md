# 聊天页业务查询结果

`index.tsx` 中的 `BusinessQueryResultContent` 展示业务查询结果，兼容实时事件及历史消息的 presentation 数据。

- 有列定义时保留表头；结果行为空时，表格内跨列居中显示“暂无数据”。
- 有结果行时按原始展示值渲染，金额为零不视为空数据。
- 没有列定义时仍沿用文本回退展示，不把解析失败直接当成零行查询结果。

验证场景：空结果、有数据结果、零金额结果，以及历史空结果消息重新打开。
